extends: theme-check:theme-app-extension
AssetSizeAppBlockJavaScript:
  enabled: true
  # 10KB default exceeded by 0.4KB even fully minified; per-block JS split is the tracked fix.
  thresholdInBytes: 15000
