# Encore QA Report — 2026-08-31 (full pass: live walk + code audit + fixes)

Scope: live admin walk in Chrome on dev-novasolutions, storefront/proxy endpoint checks, and an exhaustive code-level design-consistency + bug audit of all ~20 admin routes. Every fix below is implemented, gated (typecheck baseline-clean, prod build green, 33/33 tests), and written to your Mac — **one `git push` deploys all of it**.

## 1. Live verification (what's confirmed working in production)

- Fix pass from earlier today IS deployed: real KPI deltas ("—" at zero history), "Welcome to Encore" naming, merchant-language outbox banner.
- New R1 endpoints work through Shopify's signed proxy: `/apps/encore/config` (with trigger + countdown dates) and `/apps/encore/badges`.
- All nav pages load; PKR currency correct everywhere; privacy/terms live.
- Built-in scheduler is running (outbox retry attempts visible).

## 2. Needs your 30-second manual check (automation limitation)

My browser automation's clicks stopped registering inside the app frame partway through (they worked earlier — picker opened on the campaign form). So these three need a human click before we trust them:

1. Dashboard → "New preorder" (top right)
2. Dashboard blue banner → "Get help"
3. Onboarding → "Choose products" (the picker)

If all three work for you: the earlier "dead buttons" finding was an automation artifact and the onboarding fallback banner I added is harmless insurance. If any fails for you too: tell me which, that's a P0 and I'll dig in with you at the keyboard.

## 3. CRITICAL bugs found by the code audit — ALL FIXED in this pass

The campaign detail page (the page a Shopify reviewer is guaranteed to open) was showing merchants **fabricated data**:

| # | Was | Now |
|---|---|---|
| 1 | Fake activity feed: invented customers, "$214 total", your Gmail address, "April 18" | Honest "Activity tracking is coming soon" state |
| 2 | Fake forecast: "18 units/day", "584 units", "±42", "auto-recalculated every 15 min", "Wedge" badge | Real sales pace computed from actual units/days since launch; "—" when no sales |
| 3 | Fake conversion funnel (12,481 views → 184 orders, hardcoded) | Removed; "Conversion analytics coming soon" |
| 4 | Deposit KPI always said "20% of total" regardless of config; balance claimed "auto-charge" always | Derived from the real deposit config; auto-charge claimed only when the deferred selling plan is actually live |
| 5 | "Mark cohort ready" toasted success but did nothing (server no-op) | Actually updates cohort status; toast only after real success |
| 6 | "Email cohort", "Capture balances", bulk Email/Retry/Refund — all "Demo: would…" stubs; Refund showed a real "cannot be undone" confirm before doing nothing | Removed |
| 7 | "View on storefront" opened the wrong URL (admin origin) | Opens your real shop |
| 8 | Cohort progress showed 100% when no target was set | Bar hidden without a target; shows real units sold |
| 9 | Settings: Klaviyo/Omnisend/Slack "Connect" = fake toast, while real Klaviyo OAuth exists | Klaviyo → real /klaviyo/connect; others honest "Coming soon" |
| 10 | Settings "Wipe data": destructive confirm → fake toast | Removed (GDPR purge already real server-side) |
| 11 | Dashboard claimed "Auto-refreshes every 60 seconds" (no polling exists) | Claim removed |
| 12 | Low-stock could never be turned OFF persistently (save hidden when disabled) | Turn off now saves immediately; Save always visible |
| 13 | Waitlist "Demo data" banner claimed Notify is fake — it's real | Banner deleted |
| 14 | 7 banners had fake ✕ dismiss buttons that did nothing | Dead ✕ removed |
| 15 | Demo collections/markets ("New Arrivals (24)") selectable in real forms — could persist fake IDs into live rules | Real lists only; honest "No collections/markets found" when empty |
| 16 | "Read the docs" links opened the in-app help form in a top-level tab (breaks embedded session) | In-app "Get help" links |
| 17 | Campaign form preview ignored live button-text edits; "Units sold" column hardcoded "0 units" | Preview live; column shows "—" instead of lying |
| 18 | Translations page asserted language-publish state from hardcoded demo locales | Copy softened to true-either-way advice |

## 4. Design-consistency findings — remaining backlog (MEDIUM, not blocking)

Recorded for the next polish pass (mostly cosmetic; none show false data):

- ~70 user-visible strings bypass t() (badges, toasts, confirms, relative times, resourceNames) — needs one i18n sweep + catalog backfill.
- "campaign"/"cohort ID"/"MOQ gated" jargon leaks in a few places; "preorder" vs "pre-order" spelling varies (plans/benchmark/settings).
- Status badge colors differ per page (Ended = grey here, red there) — needs one shared statusToTone.
- Two different relativeTime helpers, English-only — should be one Intl.RelativeTimeFormat helper.
- 5 window.confirm dialogs (untranslated) — should be Polaris modals.
- Inline hex colors (#F1F1F1, #E1E3E5) instead of Polaris tokens in demand chart + previews (breaks dark mode).
- A11y: focus outlines removed via all:unset on 3 custom controls; checklist state is color-only; preview mock buttons are focusable.
- Benchmark page uses internal pilot language ("validation-gate scorecard", "the gate").
- Sample translations count toward the "n/8 translated" badge.

## 5. Environment notes (not app bugs)

- **Outbox pending grew 3 → 4 during the walk**: the scheduler is retrying but the Nova platform backend (NOVA_API target) appears unreachable — support tickets and billing sync events will queue until Nova is deployed/running. Decide: bring Nova up, or accept queuing until then (nothing is lost; retries cap at 6h backoff).
- Storefront tests still blocked by store setup (Debut theme, unpublished campaign product) — prep steps are in GO-LIVE-AND-TEST-GUIDE.md §2.
- Dashboard iframe first paint took 5–10s on cold load; acceptable for review but worth a look later (loader does ~6 queries serially via Promise.all — it's parallel already; likely Railway cold start).

## 6. Verdict

Admin quality after this pass: honest everywhere I could verify — no fabricated numbers, no fake-success actions, no dead controls known to remain (pending your 3-click check). The gap to "submission-quality" is now: your 3 manual clicks, the storefront walk after theme/campaign prep, and the MEDIUM polish backlog above (post-submission is fine for most of it).
